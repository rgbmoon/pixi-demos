/** Число буферных слотов над видимой зоной по умолчанию: в них слот меняет значение за маской. */
export const DEFAULT_BUFFER = 1

/**
 * Допуск на верхней границе диапазона ленты, в единицах длины модели: позиция ближе него к границе
 * сворачивается в начало диапазона. Без допуска сторону границы определяет ошибка округления `%`.
 */
export const WRAP_EPSILON = 1e-6
