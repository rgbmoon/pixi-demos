/** Ячеек сверх видимых по умолчанию: запас, внутри которого слот успевает сменить значение вне зоны. */
export const DEFAULT_BUFFER = 1

/**
 * Допуск на границе диапазона ленты, в единицах машины.
 * Точка покоя слота может попасть ровно на границу, и с какой её стороны окажется результат `%`,
 * решает порядок ошибок округления. Допуск выбран много меньше пикселя и много больше двойной точности.
 */
export const WRAP_EPSILON = 1e-6
