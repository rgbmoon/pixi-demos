# Адрес игрового WebSocket: реального сервера нет, значение перехватывает MSW. Оно обязано совпадать
# у транспорта и у мок-хендлера, поэтому дефолт лежит в репозитории; локально его правит .env.local
VITE_WS_URL=ws://localhost/game
