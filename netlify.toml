# Сборку и деплой ведёт GitHub Actions: publish-каталог читает netlify-cli при загрузке готового
# dist, команда сборки нужна только на случай сборки на стороне Netlify.
[build]
  command = "pnpm build:mocks"
  publish = "web/dist"

[build.environment]
  NODE_VERSION = "22"
