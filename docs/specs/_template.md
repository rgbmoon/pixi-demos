# <Название модуля/фичи>

> Одно-два предложения: что это и где живёт в коде (пути `src/...`).

## Назначение

Что делает модуль на уровне продукта/игры.

## Зачем (какую задачу решает)

Проблема или потребность, ради которой модуль существует. Почему он вообще нужен и почему стоит там, где стоит.

## Бизнес-логика и принципы работы

Правила и принципы: как модуль себя ведёт, из каких шагов состоит поток, какие есть состояния/режимы. Акцент на **почему так**, а не на пересказ кода. Неочевидные решения — с обоснованием.

## Эвенты / контракты

Входные и выходные эвенты, данные из сети/сторов, публичный API модуля. *(Пока эвентная модель и сеть не реализованы — заполнять по мере появления.)*

## Связи и зависимости

С чем взаимодействует: сторы, другие модули, ассеты, утилиты. Кто его дёргает и кого дёргает он.

## Открытые вопросы

Что ещё не решено или требует уточнения у Сергея.
